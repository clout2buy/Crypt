# Local config & secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Editor / OS
.idea/
.vscode/
.claude/
.DS_Store
Thumbs.db

# Local-only experiments not part of Crypt itself
scratch/
_stage/

# User auth (already lives in ~/.crypt/, never in repo, but belt + suspenders)
.crypt/
auth.json
